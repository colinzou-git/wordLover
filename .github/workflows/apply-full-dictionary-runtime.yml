name: Apply full dictionary runtime integration (archived)

on:
  workflow_dispatch:

jobs:
  archived:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "The sharded full dictionary runtime has already been integrated."
