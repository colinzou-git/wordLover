Overview

This app helps English learners memorize vocabulary words and short phrases, including spelling, English meaning, Chinese meaning, IPA American pronunciation marks when available, and audio pronunciation. It can instantly translate an English word or short phrase to Chinese meaning and save user-specific vocabulary and quiz progress. Each requirement is marked with a unique ID so it can be tracked clearly.

| ID  | Priority | Issue ID | Status | Category   | Description |
| --- | -------- | -------- | ------ | ---------- | ----------- |
| 1   | 1        |          |        | input      | User inputs one vocabulary term at a time. A term can be a single English word or a short phrase up to 6 words. Letters, spaces, hyphens, and apostrophes are allowed; numbers and other punctuation are not allowed. Uppercase letters and repeated spaces should be normalized for dictionary lookup and duplicate checking. |
| 2   | 1        |          |        | input      | While the user inputs the term, the app automatically searches the backend dictionary for an exact normalized match. The app only proposes meanings when the input exactly matches a dictionary word or phrase. |
| 3   | 1        |          |        | dictionary | When the dictionary finds an exact match, the app automatically displays the English meaning, Chinese meaning, IPA American pronunciation marks when available, and an audio playback button. |
| 4   | 1        |          |        | dictionary | If the term has multiple meanings, the app displays the top three meanings in order of usage frequency, with the most frequently used meaning first. |
| 5   | 1        |          |        | input      | After the user inputs a term, the user presses a finish button to save or complete the term entry. |
| 6   | 1        |          |        | input      | If the input term does not match any word or phrase in the dictionary, the app pops up a warning dialog and asks whether the user wants to save the term anyway. |
| 7   | 1        |          |        | input      | For an unknown dictionary term, the warning dialog allows the user to save anyway, edit the term, cancel, and manually input the meaning before saving. |
| 8   | 1        |          |        | input      | If the user inputs a term that already exists in the user's saved vocabulary list, the app pops up a duplicate-term dialog, tells the user the term is already saved, and ignores the new duplicate entry. |
| 9   | 1        |          |        | dictionary | Before saving, the user can manually correct or edit the dictionary-provided meaning and pronunciation information if the result is wrong or incomplete. |
| 10  | 2        |          |        | input      | Dictionary search should feel very fast. If lookup takes noticeable time, the app displays a loading or search-in-progress state. |
| 11  | 1        |          |        | vocabulary | Each user has exactly one vocabulary list for now. The app automatically creates this list for the user the first time the user saves a vocabulary term. |
| 12  | 1        |          |        | vocabulary | After a new term is saved, it is added to the user's vocabulary list. The app records the date and time when the term was saved. |
| 13  | 1        |          |        | account    | The app supports multiple users. Users register and sign in with a Google account. |
| 14  | 1        |          |        | storage    | The app saves user-specific information, including the user's vocabulary list and quiz review progress, to the user's Google Drive account. |
| 15  | 1        |          |        | storage    | When the user logs in again, the app automatically reloads the user's saved vocabulary list and quiz progress. |
| 16  | 1        |          |        | storage    | The app keeps a local copy of user data on the device and uses the local copy by default. |
| 17  | 1        |          |        | sync       | The app syncs the local vocabulary list and quiz progress with the user's Google Drive account so the same user can use multiple devices and keep data consistent across devices. |
| 18  | 1        |          |        | vocabulary | The vocabulary list displays saved terms in a clear list view. Each row shows the term, Chinese meaning, English meaning when available, pronunciation when available, and saved date. |
| 19  | 1        |          |        | vocabulary | The vocabulary list is sorted by saved date by default, with the newest saved term shown first. |
| 20  | 1        |          |        | vocabulary | The vocabulary list supports opening a saved term to view its full details, including all saved meanings, pronunciation information, dictionary source information, and review status. |
| 21  | 1        |          |        | vocabulary | The user can edit a saved vocabulary term's Chinese meaning, English meaning, and pronunciation information after the term has been saved. |
| 22  | 1        |          |        | vocabulary | When the user edits a saved vocabulary term's meaning or pronunciation, the app preserves the original dictionary-provided values separately from the user's edited values. The app displays the user's edited values by default. |
| 23  | 1        |          |        | vocabulary | The user can save a term even if the dictionary has incomplete information, as long as the user provides at least one meaning before saving. |
| 24  | 2        |          |        | vocabulary | The vocabulary list supports searching or filtering saved terms by term text, Chinese meaning, and English meaning. |
| 25  | 2        |          |        | vocabulary | The vocabulary list shows an empty state when the user has not saved any terms yet. The empty state should guide the user to add the first vocabulary term. |
| 26  | 1        |          |        | review     | Saved vocabulary terms are used later for quiz review to help the user memorize the words and phrases. |
| 27  | 1        |          |        | dictionary | The app can be used as a dictionary, not only as a vocabulary-saving tool. The main search input supports looking up words and short phrases even before the user decides whether to save them. |
| 28  | 1        |          |        | dictionary | As the user types in the search input field, the app automatically searches the dictionary and shows the closest matching term. The closest match should be based on normalized term text and should prefer exact matches over prefix or fuzzy matches. |
| 29  | 1        |          |        | dictionary | For the closest matching dictionary term, the app displays all available details, including the term text, English meaning, Chinese meaning, pronunciation marks when available, audio playback when available, dictionary source information, and whether the term is already saved in the user's vocabulary list. |
| 30  | 1        |          |        | dictionary | The dictionary result includes a save button that lets the user manually save the searched term to the user's vocabulary list. If the term is already saved, the save button is replaced by a saved state or disabled state. |
| 31  | 1        |          |        | dictionary | If the user manually saves a dictionary search result, the saved vocabulary item uses the displayed dictionary term, normalized term key, English meaning, Chinese meaning, pronunciation information, and dictionary source information. |
| 32  | 1        |          |        | settings   | The app has an autosave setting for dictionary searches. Autosave is enabled by default. |
| 33  | 1        |          |        | dictionary | When autosave is enabled and a dictionary search matches a dictionary entry, the app automatically saves the searched term to the user's vocabulary list if the user takes no other action. |
| 34  | 1        |          |        | dictionary | Autosave must not create duplicate vocabulary entries. If the matched search term is already in the user's vocabulary list, autosave does nothing. |
| 35  | 1        |          |        | dictionary | When autosave is disabled, dictionary searches do not automatically save to the user's vocabulary list. The user can still save a matched search result manually with the save button. |
| 36  | 1        |          |        | dictionary | If a search does not match any dictionary entry, the app does not autosave the search input to the user's vocabulary list. |
| 37  | 1        |          |        | search     | When the user focuses the search input field, the app automatically shows the user's ten most recent valid dictionary search history items. |
| 38  | 1        |          |        | search     | A search history item is valid only when the searched term matched an actual dictionary entry at the time of search. Search inputs that do not match any dictionary item are not saved to search history. |
| 39  | 1        |          |        | search     | Search history stores the dictionary-matched term, not the raw unmatched user input. For example, if fuzzy search resolves the input to a closest dictionary term, the matched dictionary term is saved as history. |
| 40  | 1        |          |        | search     | Search history is ordered by most recent successful search first and keeps at most the latest ten items for quick display on search focus. |
| 41  | 2        |          |        | search     | Selecting a search history item fills the search input with that term and immediately displays the dictionary details for that term. |
| 42  | 1        |          |        | settings   | The autosave setting persists for the signed-in user and is restored when the user opens the app again. |
| 43  | 1        |          |        | home       | When the user opens the app, the first screen shows the search input field at the top. The user can immediately search any supported word or short phrase from this field. |
| 44  | 1        |          |        | home       | The middle area of the first screen shows today's vocabulary study stats to motivate continued learning and review. |
| 45  | 1        |          |        | stats      | Today's study stats include the number of new terms saved to the user's vocabulary list today. |
| 46  | 1        |          |        | stats      | Today's study stats include the number of existing vocabulary terms reviewed today. |
| 47  | 1        |          |        | stats      | Today's study stats include the number of vocabulary terms that became solidly remembered today. |
| 48  | 1        |          |        | review     | If any saved vocabulary terms are due for review, the first screen enables a review button that lets the user start reviewing due vocabulary terms. |
| 49  | 1        |          |        | review     | If no saved vocabulary terms are due for review, the review button is disabled or hidden so the user does not start an empty review session. |
| 50  | 1        |          |        | study      | The first screen provides a button that lets the user proactively study one more new word. |
| 51  | 1        |          |        | study      | The proactive new-word study button selects a candidate from the dictionary using the most frequently used words that are not already in the user's vocabulary list. |
| 52  | 1        |          |        | study      | The proactive new-word study flow can be repeated multiple times in one day. The app does not set a daily hard limit; the user decides how many new words to study. |
| 53  | 1        |          |        | study      | When the user clicks the proactive new-word study button, the app first presents a quiz for the candidate word instead of immediately adding the word to the vocabulary list. |
| 54  | 1        |          |        | quiz       | The proactive new-word quiz is a multiple-choice question. The quiz should test whether the user already knows the candidate word before it is saved as new vocabulary. |
| 55  | 1        |          |        | quiz       | If the user answers the proactive new-word quiz correctly on the first attempt, the candidate word is treated as already known and is not added to the user's vocabulary list. |
| 56  | 1        |          |        | quiz       | If the user does not answer the proactive new-word quiz correctly on the first attempt, the candidate word is treated the same as a successful dictionary search from the search input field. |
| 57  | 1        |          |        | vocabulary | A proactive study word that the user misses on the first quiz attempt follows the same save and autosave rules as a searched dictionary term, including duplicate prevention and respect for the autosave setting. |
| 58  | 1        |          |        | stats      | A proactively studied word only counts as a new term saved today if it is actually added to the user's vocabulary list. Words answered correctly on the first attempt do not increase the new-saved count. |
| 59  | 1        |          |        | quiz       | After the proactive new-word quiz, the app shows feedback indicating whether the user already knew the word or whether the word should be studied and saved. |
| 60  | 1        |          |        | platform   | The app must be portable and installable on iPhone, iPad, Android phones, Android tablets, and Windows PCs. |
| 61  | 1        |          |        | platform   | The app must not require an additional paid subscription or app-specific fee for the core dictionary, vocabulary list, review, stats, and local study features. |
| 62  | 1        |          |        | offline    | The app must be local-first. Dictionary lookup, vocabulary list access, vocabulary editing, review sessions, study stats, and proactive new-word study must work without an internet connection after the app and local dictionary data are installed. |
| 63  | 1        |          |        | offline    | When the device is offline, the app uses local dictionary data and local user data by default. Offline user changes are saved locally and queued for cloud sync when internet access is available again. |
| 64  | 1        |          |        | storage    | User-specific information is stored separately from the bundled dictionary data. User-specific information includes the vocabulary list, user-edited meanings, review progress, study stats, search history, settings, and sync metadata. |
| 65  | 1        |          |        | sync       | The app supports cloud synchronization of user-specific information so the same user can sign in on different supported devices and see the same vocabulary list, study progress, stats, search history, and settings. |
| 66  | 1        |          |        | sync       | Google Drive is the first supported cloud storage target for syncing user-specific information. The sync design should allow other cloud storage providers to be added later. |
| 67  | 1        |          |        | sync       | Cloud sync must not be required for local use. If the user does not sign in or does not have internet access, the app remains fully usable with local data on the current device. |
| 68  | 1        |          |        | sync       | When the same user uses multiple devices, cloud sync should merge user-specific changes and preserve the latest vocabulary list, review progress, stats, search history, and settings across devices. |
| 69  | 1        |          |        | sync       | If a sync conflict occurs for the same saved vocabulary term, the app preserves user-edited meanings and pronunciation data and resolves conflicts without deleting user-created content silently. |
| 70  | 1        |          |        | ai         | Whenever the UI displays a word or phrase, whether in dictionary results, the vocabulary list, vocabulary details, quizzes, review results, or proactive study, the UI provides a button to view more detailed AI-assisted information. |
| 71  | 1        |          |        | ai         | The AI-assisted detail button opens an online ChatGPT-powered word detail experience using the user's ChatGPT account when available. This feature requires internet access and is separate from the app's offline core functionality. |
| 72  | 1        |          |        | ai         | The app must remain usable without the AI-assisted detail feature. If the user is offline or not connected to a ChatGPT account, the app should keep showing local dictionary information and explain that AI-assisted details require an online ChatGPT connection. |
| 73  | 1        |          |        | ai         | The AI-assisted word detail view automatically displays example sentences for the selected word or phrase. For each distinct meaning shown to the user, the AI detail view provides two example sentences. |
| 74  | 1        |          |        | ai         | AI-generated example sentences should be appropriate for English learners, clearly connected to the selected meaning, and should avoid confusing the user with unrelated meanings. |
| 75  | 1        |          |        | ai         | The AI-assisted word detail view allows the user to ask follow-up questions about the selected word or phrase. |
| 76  | 2        |          |        | ai         | The AI-assisted word detail view provides quick-action buttons for common follow-up questions, including asking about the history or origin of the word, the best or most natural usage, and the most frequently used meaning. |
| 77  | 2        |          |        | ai         | The AI-assisted word detail view may provide additional quick-action buttons for pronunciation tips, common mistakes, differences between similar words, and phrase usage when relevant. |
| 78  | 1        |          |        | ai         | AI-assisted information is supplemental and does not overwrite the local dictionary data or the user's saved vocabulary meanings unless the user explicitly chooses to copy or save AI-provided content. |
| 79  | 1        |          |        | ai         | If the user saves AI-provided content into a vocabulary item, the app records that the content came from an AI-assisted source so the user can distinguish it from dictionary-provided and user-entered content. |
| 80  | 1        |          |        | diagnostics | The app must be debuggable. It records enough local diagnostic logs to investigate app crashes, sync failures, dictionary lookup failures, data corruption warnings, review-session problems, and unexpected user-data changes. |
| 81  | 1        |          |        | diagnostics | Diagnostic logs must avoid exposing sensitive user content whenever possible. If logs need to reference user data, they should prefer stable IDs, counts, timestamps, sync versions, and error codes instead of full vocabulary meanings or private user-entered text. |
| 82  | 1        |          |        | diagnostics | The app provides a user-accessible action to automatically generate a diagnostic log bundle. The bundle includes recent app logs, device/app version information, database schema version, sync state metadata, recent error summaries, and checkpoint metadata. |
| 83  | 1        |          |        | diagnostics | The diagnostic log bundle is compressed into a single archive file before sharing or uploading. |
| 84  | 1        |          |        | diagnostics | The app provides a button to upload the compressed diagnostic log bundle to a configured Git repository for troubleshooting. |
| 85  | 1        |          |        | diagnostics | Uploading a diagnostic log bundle requires explicit user action and should clearly show what type of information will be included before upload. |
| 86  | 2        |          |        | diagnostics | If the device is offline when the user tries to upload a diagnostic log bundle, the app saves the compressed bundle locally and allows the user to retry upload later. |
| 87  | 1        |          |        | backup     | The app must protect user-specific information from corruption. User-specific information includes vocabulary list, user-edited meanings, study progress, stats, search history, settings, and sync metadata. |
| 88  | 1        |          |        | backup     | The app creates local user-data checkpoints so the user can restore to a known-good version if current user data becomes corrupted or unusable. |
| 89  | 1        |          |        | backup     | The app keeps at least one daily checkpoint for recent user data, including a checkpoint that represents the last known-good state before the current day when available. |
| 90  | 1        |          |        | backup     | Before applying cloud sync changes, database migrations, bulk vocabulary imports, or other high-risk data operations, the app creates a checkpoint of the current local user data. |
| 91  | 1        |          |        | backup     | Each checkpoint records metadata including creation time, app version, database schema version, user account ID, device ID, sync version, and a data-integrity summary. |
| 92  | 1        |          |        | backup     | The app validates user-data integrity after writes, sync operations, migrations, and restore operations. If validation fails, the app warns the user and offers restoration from a known-good checkpoint. |
| 93  | 1        |          |        | backup     | The app provides a rollback flow that lets the user restore user-specific data from a selected known-good checkpoint. The rollback must not modify the bundled dictionary data. |
| 94  | 1        |          |        | backup     | Before rollback, the app creates a final checkpoint of the current possibly-corrupted state so support can investigate and the user can undo the rollback if needed. |
| 95  | 1        |          |        | backup     | After rollback, the app revalidates the restored user data and clearly indicates whether the restore succeeded. |
| 96  | 1        |          |        | backup     | Checkpoints can be included in cloud sync or cloud backup so the same user can recover data on a new device, but local restore must still work without internet access when local checkpoints are available. |
| 97  | 2        |          |        | backup     | The app should retain multiple recent checkpoints while controlling storage usage. Older checkpoints may be pruned according to a retention policy after newer known-good checkpoints exist. |
